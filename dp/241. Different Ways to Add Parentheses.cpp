class Solution {
public:
    std::vector<int> diffWaysToCompute(std::string expression) {
        for (int i = 0; i < expression.length(); i++) {
            if (expression[i] == '-' || expresssion[i] == '*' || expression[i] == '+') {
                std::string 
            }
        } 
        
    }
};
