#include <bits/stdc++.h>

class Solution {
public:
    void moveZeroes(std::vector<int>& nums) {
        for(int num:nums) {
            if(num == 0) {
                nums.push_back(num);
                nums.
            }
        }
    }
};